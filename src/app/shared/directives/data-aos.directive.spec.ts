import { DataAosDirective } from './data-aos.directive';

describe('DataAosDirective', () => {
  it('should create an instance', () => {
    const directive = new DataAosDirective();
    expect(directive).toBeTruthy();
  });
});
